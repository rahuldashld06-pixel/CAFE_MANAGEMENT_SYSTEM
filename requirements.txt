Flask>=3.0,<4
mysql-connector-python>=9.0,<10
Werkzeug>=3.0,<4
python-dotenv>=1.0,<2
gunicorn>=23,<24
razorpay>=1.4,<2
requests>=2.31,<3
# Pure Python, SVG output, no image library needed.
qrcode>=7.4,<9
# The time-zone database. Windows ships none at all, and a container
# may not either - without this a cafe cannot be told what o clock it is.
tzdata>=2024.1
